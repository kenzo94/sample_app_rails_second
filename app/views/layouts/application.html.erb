<!DOCTYPE html>
<html>
  <head>
    <!-- full_title methode in der application_helper.rb file -->
    <title><%= full_title(yield(:title)) %></title>
    <%# Das sind Partials %>
    <%= render 'layouts/default_header' %>
    <%= render 'layouts/shim' %>
  </head>
  <!-- IFELSE for old Internet Explorer ver.  -->

  <body>
    <%= render 'layouts/header' %>

      <div class="container">
        <%= yield %>
        <%= render 'layouts/footer' %>
      </div>
    </body>

</html>
